import 'package:flutter/material.dart';
class OffersSubmittedScreen extends StatelessWidget {
  const OffersSubmittedScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          SizedBox(height: 40,),

          Container(color: Colors.orange,width: 10,height: 10,)
        ],
      ),
    );
  }
}
